/**
 * App entry. Importing the page pulls in the whole component tree
 * (template → organisms → molecules → atoms) via side-effect registration.
 */
import './components/pages/practice-page/et-practice-page.js';
